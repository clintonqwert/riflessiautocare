node_modules/
.next/
*.tsbuildinfo
.env*
.DS_Store
.claude/worktrees/
# per-machine Claude settings — differs between the workstation and the MacBook
.claude/settings.local.json
.vscode
.devswarm-temp/
# local session scratch — never committed (builder skill)
HANDOFF.md
!.env.example
.vercel

# Untouched source photos — the optimiser regenerates the web versions from
# these, so they must not ship. Keep your own backups elsewhere too.
public/gallery/_originals/
